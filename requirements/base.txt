# Core runtime dependencies
# Shared across all environments

# Web framework
Flask>=3.0.0
flask-socketio>=5.3.0
# Signed chat-session tokens (app/websocket/chat.py). A Flask dependency, but
# imported directly here, so pinned explicitly.
itsdangerous>=2.1.0

# Configuration
python-dotenv>=1.0.1
PyYAML>=6.0.1

# HTTP client
requests>=2.31.0

# System monitoring
psutil>=5.9.0

# Computer vision
opencv-python>=4.8.0
scikit-image>=0.22.0
numpy>=1.26.0
