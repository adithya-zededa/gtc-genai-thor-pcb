# ZEDEDA Camera Monitoring Agent - Dependencies
#
# This file installs all dependencies (production + testing).
# For targeted installs, use the files under requirements/:
#   pip install -r requirements/base.txt   # Core runtime only
#   pip install -r requirements/prod.txt   # Production (base + gunicorn)
#   pip install -r requirements/dev.txt    # Development (base + test + linting)
#   pip install -r requirements/test.txt   # Testing (base + pytest)

-r requirements/prod.txt
-r requirements/test.txt
